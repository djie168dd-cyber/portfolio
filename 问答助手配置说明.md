# 简历问答助手（RAG）配置说明

作品集左下角的「问我」是一个真实 RAG 问答入口：提问后，服务器先在本地简历知识库
（`rag/knowledge-base.json`）中检索相关片段，再调用大模型生成回答，并标注参考来源。

## 一、组成

- `rag/knowledge-base.json`：知识库内容（来自简历与作品集，可自行增删改）。
- `server.js` 中的 `/api/ask`：本地检索 + 调用 OpenAI 兼容大模型接口。
- `chat-widget.js` / `chat-widget.css`：页面左下角悬浮问答入口与对话面板。

## 二、配置大模型（二选一）

问答默认使用 DeepSeek（OpenAI 兼容协议），也可换成通义千问、OpenAI、Kimi 等任意兼容服务。

### 方式 A：本地配置文件（最简单，适合双击启动）

1. 复制 `rag/llm-config.example.json`，在同目录另存为 `rag/llm-local.json`。
2. 填入你自己的 Key：

```json
{
  "apiKey": "sk-你的key",
  "baseUrl": "https://api.deepseek.com",
  "model": "deepseek-chat"
}
```

3. 重新启动网站即可。`llm-local.json` 已加入 `.gitignore`，且服务器禁止通过网页访问该文件，不会泄露。

> 其他兼容服务示例：
> - 通义千问（DashScope 兼容模式）：`baseUrl` 用其 OpenAI 兼容地址，`model` 如 `qwen-plus`。
> - OpenAI：`baseUrl` 为 `https://api.openai.com/v1`，`model` 如 `gpt-4o-mini`。
> - Kimi（Moonshot）：`baseUrl` 为 `https://api.moonshot.cn/v1`，`model` 如 `moonshot-v1-8k`。

### 方式 B：环境变量（适合部署到服务器）

```bash
# Windows（CMD / 启动脚本）
set LLM_API_KEY=sk-你的key
set LLM_BASE_URL=https://api.deepseek.com
set LLM_MODEL=deepseek-chat
node server.js

# macOS / Linux
LLM_API_KEY=sk-你的key LLM_MODEL=deepseek-chat node server.js
```

环境变量优先级高于本地配置文件。

## 三、未配置 Key 时的表现

网站、案例、文档链接等所有静态内容完全正常；仅问答接口会返回「尚未配置 API Key」的提示，
前端会显示友好说明，不影响其他功能。

## 四、更新知识库

直接编辑 `rag/knowledge-base.json`，每个片段包含 `category`、`title`、`tags`、`content`。
保存后重启 `server.js` 生效（启动时一次性载入内存）。问答质量主要取决于这里的内容，
建议把希望招聘方了解、但页面放不下的细节都补充进去。

## 五、部署提示

- 该功能依赖 Node 服务端（`server.js`），不能只用纯静态空间；可部署到任意 Node 主机
  （自有服务器、Render、Railway、腾讯云等），并用方式 B 注入环境变量。
- 切勿把含真实 Key 的 `llm-local.json` 提交到公开仓库或随文件夹外发。

## 六、部署到 Render（免费，推荐：让线上网站也能问答）

GitHub Pages 只能托管静态文件，问答接口跑不起来。Render 免费 Web Service 可以直接运行
`server.js`：**网站页面和 `/api/ask` 在同一个域名下提供服务**，前端无需任何改动，
页面由服务端注入启用标记，问答入口自动出现。仓库根目录已备好 `render.yaml` 蓝图配置。

### 部署步骤

1. 注册 / 登录 <https://render.com>（可用 GitHub 账号登录）。
2. 控制台点 **New → Blueprint**，选择本仓库（`djie168dd-cyber/portfolio`）。
   - 若列表里没有该仓库，点 **Configure account** 给 Render 授权访问。
   - 注意：Blueprint 读取的是**已推送到 GitHub 的 render.yaml**，部署前请确认
     `render.yaml` 已提交推送（见文末自查清单）。
3. Render 识别到 `render.yaml` 后，会提示填写唯一的密钥环境变量 **`LLM_API_KEY`**
   （火山方舟的 Key；`LLM_BASE_URL`、`LLM_MODEL` 已在蓝图中预填豆包 turbo）。
   - 该变量标记为 `sync: false`，**只存在 Render 后台，不会写进仓库**。
4. 点 **Apply / Create Service**，等待首次构建部署完成（约 1–3 分钟）。
5. 部署成功后得到一个地址，形如 `https://dengjie-portfolio.onrender.com`。
   直接把这个地址发给招聘方即可——页面、案例、问答助手全部可用。

### 后续更新代码

`render.yaml` 中 `autoDeploy: true`：之后推送到 Render 绑定的分支，会自动重新部署。
本地双击运行的方式（`启动网站.cmd` / `node server.js`）完全不受影响。

### 免费版注意事项

- **冷启动**：免费实例 15 分钟无人访问后会休眠，下次打开约需 30–60 秒唤醒，属正常现象。
- **防滥用限频**：同一 IP 每 60 秒最多 12 次问答（超出返回 429 友好提示），保护大模型额度；
  可在 Render 环境变量里调 `RATE_MAX`、`RATE_WINDOW_MS`。
- 火山方舟账户需保持余额/额度充足，否则接口会返回模型侧错误。
- 想换 DeepSeek：在 Render 环境变量把 `LLM_BASE_URL` 改为
  `https://api.deepseek.com`、`LLM_MODEL` 改为 `deepseek-chat` 即可，无需改代码。

### 与 GitHub Pages 的关系

- Pages（`*.github.io/portfolio/`）继续照常发布纯静态页面，**不显示问答入口**；
  Render 地址是功能完整版。两个地址可以并存，简历上建议放 Render 地址。
- 不要让 Pages 页面跨域调用 Render 的接口（未配置 CORS，且 Pages 无启用标记）；
  直接使用 Render 同域地址即可，无需跨域。

### 提交前自查清单

- [ ] `render.yaml`、`server.js` 已提交并推送到将被 Blueprint 读取的分支；
- [ ] 仓库中**没有** `rag/llm-local.json`（该文件已在 `.gitignore` 中）；
- [ ] Render 后台已填写 `LLM_API_KEY`，日志出现「问答助手已启用」即成功。
